package start

import (
	"FastSocketsTool/pkg/option"
)

func Start(option *option.OperationOption) {
	if *option.Server {

	} else {

	}

	//dial, err := net.Dial(option.ConnectionType, option.GetConnectionAddress())
	//if err != nil {
	//	prompt.Prompt("failed_to_connect_to_the_server")
	//	return
	//}
}
